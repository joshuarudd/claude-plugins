Capture the following feedback as GitHub issues. Read the capture-issue SKILL.md for full instructions, then use the Task tool to create the issues in a sub-agent so the main session context stays clean. Return a brief confirmation with issue numbers, titles, and URLs.

$PROMPT
